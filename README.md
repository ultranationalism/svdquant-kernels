# svdquant-kernels

Cross-architecture CUDA kernels for SVDQuant (W4A4 with low-rank correction).

Targets multiple GPU generations — not specific to a single backend — so each architecture can carry the layout, tile shape, and instruction set that actually fits it.

## Status

Scaffold. No kernels yet.

## License

Apache-2.0
